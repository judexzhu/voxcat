# Voxcat Configuration
# Persona definitions live in ~/.config/voxcat/personas/*.md
# Run `voxcat init` to create them.

voice:
  mode: "split" # "live" = all-in-one Gemini Live, "split" = separate STT + LLM + TTS
  live:
    model: "gemini-3.1-flash-live-preview"
    voice: "Aoede" # Options: Puck, Charon, Kore, Fenrir, Aoede
  split:
    stt_model: "gemini-3.5-transcribe-live"
    llm_model: "gemini-3.7-flash"
    tts_model: "gemini-3.1-flash-tts-preview"
    tts_voice: "Aoede" # Global default. Per-persona overrides in persona files.
    # tts_style: "extremely fast" # Global default. Options: extremely fast, whispering, shouting, sarcasm, robotic.

# Model used by deep_analysis, research, and summarize_session tools
tools:
  analysis_model: "gemini-3.7-flash"
  thinking_budget: 8192

# MCP servers — add your own here.
# Example:
#   my-server:
#     command: "uv"
#     args: ["run", "python", "-m", "my_mcp_server"]
#     cwd: "/path/to/my-mcp-server"
#     env_keys: [MY_API_TOKEN]
#     read_only_tools:
#       - search_items
#       - get_item
mcp_servers: {}

persona:
  default: "thinking-partner"

server:
  host: "0.0.0.0"
  port: 7860
